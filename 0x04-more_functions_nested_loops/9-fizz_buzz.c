#include <stdio.h>

/**
 * main -print fizzbuzz
 * Return: o always
 *
 */

int main(void)
{
	for (n == 1; n <= 100; n++)
	{
		printf(" ");
		if ((n % 3 == 0) && (n % 5 == 0))
			printf("FIzz Buzz\n");
		else if (n % 3 == 0)
			printf("Fizz\n");
		else if (n % 5 == 0)
			printf("Buzz\n");
		else
			printf("%d\n", n);
	}
	return (0);
}
